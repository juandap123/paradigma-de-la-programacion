let nombre = "juan";
let edad = 30;
let apellido = "perez";
let numero = 17;

function sumar(a , b) {
    return a + b;
}

function restar(a , b ) {
   let operacion = a - b;
   return operacion;
}


console.log("el resultado de la suma: " + sumar(numero, edad));
letresultadoresta = restar(5, 7);

console.log("resultado de la resta: " + resultadoresta);
console.log("nombre completo " +nombre + " " + apellido + "; edad " + edad );